# Cloudflare Pages — static site (no Functions)
# Connect this repo in Cloudflare Pages; it reads the settings below,
# so there's nothing to configure in the dashboard build UI.
name = "gettoknowthisplace"
pages_build_output_dir = "public"
compatibility_date = "2025-06-01"
